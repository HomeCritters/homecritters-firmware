<!--
Thanks for the PR! Keep it to one topic. See CONTRIBUTING.md.
-->

## What & why

<!-- What does this change, and what problem does it solve? Link the issue: Fixes #123 -->

## How it was tested

<!-- Pick what applies and delete the rest. "Built only, not flashed" is a fine, honest answer. -->

- [ ] `pio run` builds clean
- [ ] Flashed and tested on hardware (Ball V2)
- [ ] Web portal rebuilt (`npm run build` + `assets/web_to_header.py`) and the
      regenerated `include/web_index.h` is in this PR
- [ ] Tested against the Home Assistant integration

## Screenshots

<!--
Required for any visual change — with the round bezel ring drawn
(tools/hwshot.py does it by default). Anything outside the ring is invisible
on the physical screen.
-->

## Checklist

- [ ] Code and comments in English; new UI strings in Portuguese (pt-BR) until
      an [i18n layer](../blob/main/TODO.md#-internationalisation-i18n--help-wanted)
      exists — grouped per screen, not scattered inline, so they're easy to
      extract later
- [ ] No hardcoded GPIO numbers (`include/pins.h` is the source of truth)
- [ ] Any new `Renderer` screen ends with `endScreen()`
- [ ] Any new screen is registered in `SCREENS[]` + `enterScreen()`
- [ ] Any new long-running task subscribes to the watchdog and `taskreg::add`
- [ ] Generated headers were regenerated by their script, not edited by hand
- [ ] Any new audio is redistributable under MIT (see `THIRD_PARTY_NOTICES.md`)
